CSRAW

namespace UnityEditor
{

// Target build platform.
ENUM BuildTarget
	// Build a web player.
	WebPlayer = 6,
	// Build a streamed web player.
	WebPlayerStreamed = 7,

	// Build an OS X standalone (i386 only).
	StandaloneOSXIntel = 4,

	// Build an OS X standalone (x86_64 only).
	StandaloneOSXIntel64 = 27,

	// Build an OS X standalone (i386/x86_64 universal).
	StandaloneOSXUniversal = 2,

	// Build a Windows standalone.
	StandaloneWindows = 5,
	
	// Build an iPhone player
	iPhone = 9,	


	// Build a PS3 Standalone
	PS3 = 10,

	// Build a XBox Standalone 
	XBOX360 = 11,

	// Build an Android .apk standalone app
	Android = 13,

	// Build a Wii standalone
	Wii = 8,

	// was StandaloneBroadcom = 12,

	//*undocumented*
	StandaloneGLESEmu = 14,
	
	// was StandaloneGLES20Emu = 15,
	
	// Build a Web Player with the Native Client runtime.
	NaCl = 16,

	// Build a Linux standalone (i386 only).
	StandaloneLinux = 17,

	// Build a Linux standalone (i386 only).
	StandaloneLinux64 = 24,

	// Build a Linux standalone (i386/x86_64 universal).
	StandaloneLinuxUniversal = 25,

	// Build content as an swf file for playback with Adobe Flash.
	FlashPlayer = 18,
	
	// Build a Windows x86_64 standalone.
	StandaloneWindows64 = 19,
	

	// *undocumented*
	MetroPlayer = 21,

#if INCLUDE_WP8SUPPORT
	// *undocumented*
	WP8Player = 26,
#endif

	// *undocumented*
	BB10 = 28,

	// *undocumented*
	Tizen = 29,
END

CSRAW }
